# Local environment variables for Clerk (Next.js)
# Replace the placeholder values with the actual keys from https://dashboard.clerk.com (API keys / Publishable keys)
# Never commit this file to source control.

# Client-side publishable key (exposed to browser)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Server-side secret/API key (keep private)
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# MongoDB connection string used by the app. Replace the placeholder value with your
# actual connection string. Examples:
# - MongoDB Atlas: mongodb+srv://<username>:<password>@cluster0.mongodb.net/eventora?retryWrites=true&w=majority
# - Local MongoDB: mongodb://localhost:27017/eventora
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/eventora?retryWrites=true&w=majority

# Clerk webhook secret for verifying webhook requests
WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Server URL for redirects (e.g., localhost for dev, production URL for prod)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Uploadthing secret for file uploads
UPLOADTHING_SECRET=sk_live_your_uploadthing_secret_here

# Admin email addresses (comma-separated) - users with these emails can access admin panel
ADMIN_EMAILS=mohamedabu.dev@gmail.com,admin@example.com,admin2@example.com